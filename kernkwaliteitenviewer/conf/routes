# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


# Viewer:

GET		/										controllers.Viewer.layout()

GET		/map/:deelgebied/:level					controllers.Viewer.getMapHtml(deelgebied,level)

GET		/text/:deelgebied/:level				controllers.Viewer.getTextHtml(deelgebied,level)

GET     /assets/*file               		    controllers.Assets.at(path="/public", file)

GET		/routes.js								controllers.Viewer.jsRoutes()