# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


# Viewer:

GET		/										controllers.Viewer.layout()

GET		/map/:level								controllers.Viewer.getMapHtml(level)

GET		/text/:level							controllers.Viewer.getTextHtml(level)

GET     /assets/*file               		    controllers.Assets.at(path="/public", file)

GET		/routes.js								controllers.Viewer.jsRoutes()